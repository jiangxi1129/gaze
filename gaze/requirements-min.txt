# 最小可运行版（截图 + OCR + caption + 本地 jsonl log，不要音频、不要 VPS）
# Python 3.10+ on Windows
Pillow>=10.0
pywin32>=306
httpx>=0.25
python-dotenv>=1.0

# OCR（必装。靠 ONNX runtime 在 CPU 跑中文 OCR，约 130MB 模型）
rapidocr-onnxruntime>=1.3
numpy>=1.24
